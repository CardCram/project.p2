@model CardCram.Client.Models.CardViewModel

@{
    ViewData["Title"] = "Delete";
}


<h1> Are you sure you want to delete this card? </h1>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(Model => Model.Question)
        </dt>
        <dd>
            @Html.DisplayFor(Model => Model.Question)
        </dd>
        <dt>
            @Html.DisplayNameFor(Model => Model.Answer)
        </dt>
        <dd>
            @Html.DisplayFor(Model => Model.Answer)
        </dd>
        <dt>
            @Html.DisplayNameFor(Model => Model.Deck)
        </dt>
        <dd>
            @Html.DisplayFor(Model => Model.Deck.Title)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="CardId"/>
        <input type="submit" value="Delete" class="btn btn-default"/>
        <a asp-action="Index" asp-route-id="@Model.DeckId"> Back To List </a>
    </form>
</div>